{
  "name": "web-ubicom",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run prepare; esbuild src/index.tsx --bundle --outfile=dist/index.js",
    "start": "npm run prepare; esbuild src/index.tsx --bundle --watch --outfile=dist/index.js;",
    "prepare": "mkdir dist; cp src/index.html dist/; ln -s $PWD/assets dist/assets",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.14.39",
    "react": "^18.1.0",
    "react-dom": "^18.1.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.9",
    "@types/react-dom": "^18.0.3",
    "typescript": "^4.6.4"
  }
}
